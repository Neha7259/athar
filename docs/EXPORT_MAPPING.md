# Export mapping status

The canonical verifier-ready CSV and HTML pack are implemented in `packages/exports`.

IEQT and EAD GHG exports intentionally remain marked `PENDING_OFFICIAL_TEMPLATE` until the official field lists are supplied. No regulator fields are invented. Once the templates are available, add versioned mapping files here and keep the ledger schema unchanged.
